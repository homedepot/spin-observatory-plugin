name: Branch
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    uses: ./.github/workflows/build.yml

  docker:
    uses: ./.github/workflows/docker-push.yml
